import { GetUserInfoResponse } from './get-user-info-response';

describe('GetUserInfoResponse', () => {
  it('should create an instance', () => {
    expect(new GetUserInfoResponse()).toBeTruthy();
  });
});
